import { StyleSheet } from "react-native";

export default (styles = StyleSheet.create({
  profilePicture: {
    width: 70,
    height: 70,
    borderRadius: 34,
    margin: 16
  },
  text: {
    textAlign: "center",
    color: "black"
  }
}));
