// reusable style props such as set colors
import { StyleSheet } from 'react-native';

export const medmindBlue = 'rgb(101, 192, 190)';
export const { drawerIconStyle } = StyleSheet.create({
  drawerIconStyle: {
    width: 20,
    height: 20,
    borderRadius: 5,
    marginLeft: '15.2%'
  }
});
